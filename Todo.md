# Todos for the project

## Refactoring
[] Refactor HydraulicPump so the class parameters make more sense, atm it is current_pressure = min_pressure WRONG

## Debug
[] Adding and removing of pumps is currently very buggy, index is ignored for some reason
[] Menu for editing is not shown yet, so its not possible to change parameters of the pumps

## Features
[] Change the add pump button to a generalized button to add any kind of generators, so any generator can exist
[] Add a start/stop/reset simulation button